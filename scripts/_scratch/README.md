# 临时脚本归档

这里存放以 `_..._tmp.py` 命名的临时分析、调试、检查脚本。

这些脚本不是稳定流水线入口，可能依赖某次调试时的本地文件、数据状态或输出格式。保留它们只是为了追溯历史分析过程。

如果某个临时脚本需要长期复用，应当：

1. 移回 `scripts/` 根层。
2. 改成符合分类规则的名称，例如 `audit_*`、`analyze_*`、`fix_*`。
3. 添加清晰的参数说明。
4. 避免依赖当前工作目录或临时文件。

